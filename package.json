{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
    },
    "devDependencies": {
        "axios": "^1.7.9",
        "laravel-vite-plugin": "^2.0.1",
        "vite": "^7.2.4"
    }
}
